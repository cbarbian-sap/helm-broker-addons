apiVersion: v1
entries:
  helm-redis:
    - name: helm-redis
      description: "Redis service"
      version: 0.0.1 
  helm-postgresql:
    - name: helm-postgresql
      description: "PostgreSQL service"
      version: 0.0.1 
